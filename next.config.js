/** @type {import('next').NextConfig} */
const nextConfig = {
  target: 'serverless',
}

module.exports = nextConfig

/**
 * const nextConfig = {
  reactStrictMode: true,
}
const nextConfig = {
  reactStrictMode: true,
}

 */